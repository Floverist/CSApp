#pragma once
#include <string>

struct Person {
	std::string card_number; //TODO: maybe change to int
	std::string card_date;   //TODO: maybe change to date type
    std::string name;//TODO: maybe expand to first name second name e.t.c.
    std::string rank; //TODO: maybe change to enum
    std::string unit; //TODO: maybe change to enum
};
